#!/bin/bash

BASEDIR=$(dirname $0)/..

source $BASEDIR/lib/my-docker.sh

$BASEDIR/bin/cygwin-x.bat

docker run \
  --rm \
  --name uc4 \
  -e DISPLAY=$DISPLAY \
  -v $MY_DOCKER/uc4:/home/$USER \
  $USER/uc4
